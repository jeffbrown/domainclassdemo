apply plugin: 'groovy'
apply plugin: 'application'

mainClassName = 'gormdemo.PersonDemo'

repositories {
    jcenter()
}

dependencies {
    runtime "com.h2database:h2:1.4.192"
    runtime "org.apache.tomcat:tomcat-jdbc:8.5.0"

    compile project(':domain')
}
